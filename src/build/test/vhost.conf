 <VirtualHost *:80>
    DocumentRoot /app/www/
    ServerName localhost

    <Directory /app/www/>
        RewriteEngine On
        Options FollowSymLinks
        AllowOverride All
        Require all granted
    </Directory>
</VirtualHost>

